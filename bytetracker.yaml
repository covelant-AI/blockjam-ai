# bytetrack.yaml
tracker_type: bytetrack
track_high_thresh: 0.5
track_low_thresh: 0.1
new_track_thresh: 0.4
track_buffer: 60  # Increase this to maintain persistence when the object disappears
match_thresh: 0.8
fuse_score: True
